# FairyGUI XML解析规范本地索引

本 Skill 已内置完整原文，不再依赖任何外部电脑路径。

完整、最高优先级的 XML 规范位于：

```text
references/fairygui-xml-parsing-specification.md
```

完整的 AI 自动生成流程原文位于：

```text
references/fairygui-ai-generation-workflow.md
```

当任务涉及 FairyGUI 全流程生产时，必须完整读取 `fairygui-ai-generation-workflow.md`。

当任务涉及生成、审查、修复或校验 FairyGUI `package.xml` / 组件 XML 时，必须完整读取 `fairygui-xml-parsing-specification.md`，不得只读取本索引文件、`SKILL.md`、摘要或短合同。

如果任一完整文档缺失、被截断或完整性校验失败，禁止继续对应阶段，并输出阻塞报告。
